<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Site Validators
    |--------------------------------------------------------------------------
    |
    | One place to store all regex for validating the input. If you use front-end
    | validation as well, make sure to sync the regex.
    |
    */

    'username' => '/(^[A-Za-z0-9\_ ]+$)+/',
];